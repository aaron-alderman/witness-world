# Thin app file. Imports are authoring projections; compilation merges into one witnessed graph.

[app]
id = "demo_todo_app"
imports = ["./demo/common.wtoml", "./demo/backend.wtoml", "./demo/frontend.wtoml"]
spawn = ["backend", "frontend"]
