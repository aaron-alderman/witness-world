[watch]
roots = ["src", "plugins", "examples/engentus", "examples/_lib"]
ignore = ["node_modules", "target", ".git", ".witness-core"]

[proof]
fast = "node --test src/runtime-surface-interaction-runtime.test.js"
full = "npm test"
slow_ms = 15000

[package]
include = ["src/**", "plugins/**", "examples/engentus/**", "examples/_lib/**"]

[supervise]
command = "node src/cli.js utility-serve examples/engentus --server engentus_server --port {runtime_port} --runtime-profile full --startup-telemetry"
working_dir = "."
restart_on_exit = true
control_url = "http://127.0.0.1:{runtime_port}/api/runtime/worker-control"
health_interval_ms = 1000
health_timeout_ms = 5000
restart_on_unhealthy = true
degraded_grace_polls = 10
unhealthy_grace_polls = 3

[frontdoor]
public_addr = "127.0.0.1:3000"
drain_timeout_ms = 15000
startup_cutover_timeout_ms = 45000
