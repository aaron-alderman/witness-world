# Runtime

Concrete runtime environment and read-model objects:

- server runner
- MCP server
- runtime preload
- materialized view

These are the objects that turn authored semantics into running environments and maintained runtime slices.
